import { createTheme } from '@material-ui/core';

const theme = createTheme({
  palette: {
    primary: {
      main: '#0079D3',
    },
    secondary: {
      main: '#f50057',
    },
  },
});

export default theme;
